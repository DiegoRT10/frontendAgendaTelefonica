export interface LOGIN {
    EMAIL: String,
    PASSWORD: String,
  }

  export interface USER {
    id: number, 
    nombre: string, 
    apellido: string,
    email: string, 
    rol?: string
  }

  